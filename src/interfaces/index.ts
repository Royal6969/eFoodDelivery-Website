import ProductInterface from "./ProductInterface";
import CartInterface from "./CartInterface";
import CartItemInterface from "./CartItemInterface";


export type { ProductInterface, CartInterface, CartItemInterface } // we have to add the right type here because we are explicity exporting the type